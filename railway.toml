[build]
buildCommand = "npm run build"

[deploy]
startCommand = "mkdir -p /app/data && NODE_ENV=production node dist/index.cjs"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
